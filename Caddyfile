{
    email fli@student.42.fr
    log {
        output file /var/log/caddy/access.log {
            roll_size 10MiB
            roll_keep 10
            roll_keep_for 720h
        }
    }
}

:8443 {
    root * /usr/share/caddy/frontend
    file_server
    encode gzip
    tls internal {
        on_demand
    }
}